<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 애플리케이션 기본 로그 레벨 설정 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- com.febrie 패키지 로그 레벨 설정 -->
    <logger name="com.febrie" level="DEBUG"/>

    <!-- com.febrie.api 패키지는 INFO 레벨로 설정 -->
    <logger name="com.febrie.api" level="INFO"/>
</configuration>
